<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.runninggo.toy.dao.PlaceMapper">

    <!-- 게시글 insert -->
    <insert id="postsInsert" parameterType="PlaceRequestDto">
        insert into course (id, local1, local2, subway_nm, level, storage, distance, desc, rcmnd_reason, img_url, del_yn, create_dt)
            values (#{id}, #{local1}, #{local2}, #{subwayNm}, #{level}, #{storage}, #{distance}, #{desc}, #{rcmndReason}, #{imgUrl}, #{delYn}, now());
    </insert>

    <!--  -->
</mapper>